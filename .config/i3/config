# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8
font pango:Ubuntu 8 

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec termite

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

#meus binds

bindsym $mod+F1 exec cat /home/duzas/linuxStuff/.config/i3/config | grep "##" | sed -E "s/^bindsym //g" | sed "s/^[-a-z ]*//g" | sed "s/exec .* ##/ /g" | sed "s/mode .* ##/ /g" | dmenu -l 5 -i -sb "#333333" ## shows this list of shortcuts

for_window [class="Unused"] rename workspace 11 to "_", move container to workspace "_"
for_window [class="Spotify"] move container to workspace 10
#for_window [class="pavucontrol"] move container to workspace 10, workspace 10, focus

#lock screen
bindsym $mod+Ctrl+l exec i3lock -i /home/duzas/bg.png -t & ## lock screen

#volume control
#bindsym XF86AudioRaiseVolume exec amixer set Master 2+
#bindsym XF86AudioLowerVolume exec amixer set Master 2-
bindsym XF86AudioRaiseVolume exec pamixer -i 2 && pkill -RTMIN+10 i3blocks
bindsym XF86AudioLowerVolume exec pamixer -d 2 && pkill -RTMIN+10 i3blocks
bindsym XF86AudioMute exec amixer set Master toggle && pkill -RTMIN+10 i3blocks

#alternative player control (for keyboards without multimedia keys)
bindsym $mod+Prior exec pamixer -i 2 && pkill -RTMIN+10 i3blocks
bindsym $mod+Insert exec pamixer -d 2 && pkill -RTMIN+10 i3blocks
bindsym $mod+Home exec amixer set Master toggle && pkill -RTMIN+10 i3blocks
#END OF alternative player control

#brightness control
bindsym XF86MonBrightnessUp exec xbacklight -inc 20
bindsym XF86MonBrightnessDown exec xbacklight -dec 20

bindsym XF86Display exec xrandr --output HDMI1 --auto & ## change video to hdmi in highest resolution

#player control
bindsym XF86AudioPlay exec playerctl -a play-pause $HOME/linuxStuff/mytools/showsong.sh ## play-pause and shows the current song name (alternative for keyboard without multimedia keys)
bindsym $mod+Control+XF86AudioPlay exec $HOME/linuxStuff/mytools/showsong.sh ## shows the current song name
bindsym XF86AudioPrev exec playerctl -a previous $HOME/linuxStuff/mytools/showsong.sh ## previous player command and shows the current song name (alternative for keyboard without multimedia keys)
bindsym XF86AudioNext exec playerctl -a next $HOME/linuxStuff/mytools/showsong.sh ## forward player command and shows the current song name (alternative for keyboard without multimedia keys)
#END OF player control

#alternative player control (for keyboards without multimedia keys)
bindsym $mod+End exec playerctl -a play-pause && $HOME/linuxStuff/mytools/showsong.sh ## play-pause and shows the current song name (alternative for keyboard without multimedia keys)
bindsym $mod+Control+End exec $HOME/linuxStuff/mytools/showsong.sh ## shows the current song name (alternative for keyboard without multimedia keys)
bindsym $mod+Delete exec playerctl -a previous && $HOME/linuxStuff/mytools/showsong.sh ## previous player command and shows the current song name (alternative for keyboard without multimedia keys)
bindsym $mod+Next exec playerctl -a next && $HOME/linuxStuff/mytools/showsong.sh ## forward player command and shows the current song name (alternative for keyboard without multimedia keys)
#END OF alternative player control

bindsym $mod+p exec playerctl -a play-pause
bindsym $mod+0x3d exec playerctl --player="vlc" position $(echo $(playerctl --player="vlc" position) + 5 | bc) ## forwards 5 seconds in vlc #plus key is 0x3d (WITHOUT SHIFT)
bindsym $mod+0x2d exec playerctl --player="vlc" position $(echo $(playerctl --player="vlc" position) - 5 | bc) ## goes back 5 seconds in vlc #minus key is 0x2d (WITHOUT SHIFT)

#mounting google drive
bindsym $mod+F11 exec /home/duzas/linuxStuff/mytools/GD.sh && pkill -RTMIN+12 i3blocks ## mounts google drive

#screenshot
#bindsym Print exec scrot -z
bindsym Print exec /home/duzas/linuxStuff/mytools/takescreenshot.sh ## takes a screenshot
#bindsym --release $mod+Print exec scrot -sz
bindsym --release $mod+Print exec killall i3-nagbar; exec "/home/duzas/linuxStuff/mytools/takescreenshot.sh Crop" ## takes a screenshot region

#simple calculator
bindsym $mod+a exec /home/duzas/linuxStuff/mytools/calc.sh ## Calculator

#managing clipboard
bindsym $mod+c exec /home/duzas/linuxStuff/mytools/sendtoclipboard.sh ## manages clipboard

#search and install from pacman
bindsym $mod+s exec /home/duzas/linuxStuff/mytools/pacmansearch.sh ## searches pacman repositories

#search and install from yaourt
bindsym $mod+Shift+s exec /home/duzas/linuxStuff/mytools/yaourtsearch.sh ## searches AUR repositories (yaourt)

#open an url
bindsym $mod+w exec /home/duzas/linuxStuff/mytools/url.sh ## searches for an url website title or _searches google

#pick a color and send to clipboard
bindsym $mod+F2 exec termite --exec="/home/duzas/linuxStuff/mytools/colorpicker.sh" --class="Unused" ## picks a color and sends to clipboard

#handle multiple monitors
bindsym $mod+F12 exec /home/duzas/linuxStuff/mytools/handlescreens.sh ## handles multiple monitors
bindsym $mod+Ctrl+F12 exec /home/duzas/linuxStuff/mytools/handleaudio.sh ## handles audio output

#Connecting to wifi profiles
bindsym $mod+F10 exec /home/duzas/linuxStuff/mytools/wifi.sh ## connects to netctl wifi profiles or opens wifi-menu

#text tools
bindsym $mod+t exec /home/duzas/linuxStuff/mytools/text.sh ## text tools
bindsym $mod+Ctrl+t exec /home/duzas/linuxStuff/mytools/text.sh "$(xsel -b)" ## text tools from clipboard

#keyboard lock keys
bindsym Caps_Lock exec sleep 0.1 && /home/duzas/linuxStuff/mytools/keylocks.sh 
bindsym Num_Lock exec sleep 0.1 && /home/duzas/linuxStuff/mytools/keylocks.sh
#bindsym Scroll_Lock exec sleep 0.1 && /home/duzas/linuxStuff/mytools/keylocks.sh


bindsym $mod+k exec /home/duzas/linuxStuff/mytools/kbmap.sh ## set keyboard map

#hue
#sudo chmod 4755 /usr/bin/beep
#bindsym $mod+z exec beep -f `xdotool getmouselocation --shell | grep X | cut -d'=' -f 2` -l 100 ## sound from mouse position hue
bindsym $mod+z exec play -qn synth 0.25 triangle `xdotool getmouselocation --shell | grep X | cut -d'=' -f 2` ## sound from mouse position hue


#FIM DE meus binds

# change focus
bindsym $mod+j focus left
#bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
#bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

mode "mouse" {

	bindsym $mod+Down exec xdotool mousemove_relative  0 10
	bindsym $mod+Up exec xdotool mousemove_relative -- 0 -10
	bindsym $mod+Left exec xdotool mousemove_relative  -- -10 0
	bindsym $mod+Right exec xdotool mousemove_relative 10 0

	bindsym $mod+Ctrl+Down exec xdotool mousemove_relative  0 50
	bindsym $mod+Ctrl+Up exec xdotool mousemove_relative -- 0 -50
	bindsym $mod+Ctrl+Left exec xdotool mousemove_relative  -- -50 0
	bindsym $mod+Ctrl+Right exec xdotool mousemove_relative 50 0

	bindsym 0x2c exec xdotool click 1 #comma
	bindsym 0x2e exec xdotool click 3 #dot

	bindsym $mod+space exec xdotool getmouselocation > /tmp/barmsg.txt && pkill -RTMIN+13 i3blocks


	bindsym $mod+Ctrl+Shift+Up exec xdotool click 4
	bindsym $mod+Ctrl+Shift+Down exec xdotool click 5

	bindsym Escape mode "default"
}

#bindsym $mod+m mode "mouse" ## mouse mode (mod+arrow keys for cursor and ,. for clicks)
bindsym $mod+m exec i3-msg mode "mouse" && killall i3-nagbar  ## mouse mode (mod+arrow keys for cursor and ,. for clicks)

mode "piano" {
	
	#sudo chmod 4755 /usr/bin/beep
	
	#C
	#bindsym $mod+q exec beep -f 262 -l 100
	bindsym $mod+q exec play -qn synth 0.2 triangle 262

	#C#
	#bindsym $mod+2 exec beep -f 277 -l 100
	bindsym $mod+2 exec play -qn synth 0.2 triangle 277

	#D
	#bindsym $mod+w exec beep -f 294 -l 100
	bindsym $mod+w exec play -qn synth 0.2 triangle 294

	#D#
	#bindsym $mod+3 exec beep -f 311 -l 100
	bindsym $mod+3 exec play -qn synth 0.2 triangle 311

	#E
	#bindsym $mod+e exec beep -f 330 -l 100
	bindsym $mod+e exec play -qn synth 0.2 triangle 330

	#F
	#bindsym $mod+r exec beep -f 349 -l 100
	bindsym $mod+r exec play -qn synth 0.2 triangle 349

	#F#
	#bindsym $mod+5 exec beep -f 370 -l 100
	bindsym $mod+5 exec play -qn synth 0.2 triangle 370

	#G
	#bindsym $mod+t exec beep -f 392 -l 100
	bindsym $mod+t exec play -qn synth 0.2 triangle 392

	#G#
	#bindsym $mod+6 exec beep -f 415 -l 100
	bindsym $mod+6 exec play -qn synth 0.2 triangle 415

	#A
	#bindsym $mod+y exec beep -f 440 -l 100
	bindsym $mod+y exec play -qn synth 0.2 triangle 440

	#A#
	#bindsym $mod+7 exec beep -f 466 -l 100
	bindsym $mod+7 exec play -qn synth 0.2 triangle 466

	#B
	#bindsym $mod+u exec beep -f 494 -l 100
	bindsym $mod+u exec play -qn synth 0.2 triangle 494

	#C
	#bindsym $mod+i exec beep -f 523 -l 100
	bindsym $mod+i exec play -qn synth 0.2 triangle 523

	#C#
	#bindsym $mod+9 exec beep -f 554 -l 100
	bindsym $mod+9 exec play -qn synth 0.2 triangle 554

	#D
	#bindsym $mod+o exec beep -f 587 -l 100
	bindsym $mod+o exec play -qn synth 0.2 triangle 587

	#D#
	#bindsym $mod+0 exec beep -f 622 -l 100
	bindsym $mod+0 exec play -qn synth 0.2 triangle 622

	#E
	#bindsym $mod+p exec beep -f 659 -l 100
	bindsym $mod+p exec play -qn synth 0.2 triangle 659



	bindsym Escape mode "default"
}

bindsym $mod+XF86TouchpadOn mode "piano" ## piano mode
bindsym $mod+Shift+Insert mode "piano" ## piano mode

set $base00 #101218
set $base01 #2f343f
set $base02 #0a0a0a
set $base03 #545b78
set $base04 #C0C5CE
set $base05 #d1d4e0
set $base06 #C9CCDB
set $base07 #ffffff
set $base08 #cf6a4c
set $base09 #f99170
set $base0A #ffefcc
set $base0B #a5ffe1
set $base0C #97e0ff
set $base0D #97bbf7
set $base0E #c0b7f9
set $base0F #fcc09e

# window Colours
#                       border  background text    indicator
client.focused          $base01 $base01    $base07 $base0F
client.focused_inactive $base02 $base02    $base03 $base0F
client.unfocused        $base02 $base02    $base03 $base0F
client.urgent           $base02 $base08    $base00 $base0F

hide_edge_borders both

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        #status_command i3status
        status_command i3blocks
	#install otf-font-awesome-4
	font pango:FontAwesome 10
        colors {
	  separator $base03
	  background $base01
	  statusline $base05
	  #                  border  background text
	  focused_workspace  $base01 $base01    $base07
	  active_workspace   $base01 $base02    $base03
	  inactive_workspace $base01 $base01    $base03
	  urgent_workspace   $base01 $base01    $base08
       }
}


